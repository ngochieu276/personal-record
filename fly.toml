# fly.toml app configuration file generated for personal-record on 2026-09-17T04:21:47Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'personal-record'
primary_region = 'sin'

[build]
  dockerfile = 'apps/api/Dockerfile'

[deploy]
  strategy = 'immediate'

[env]
  NODE_ENV = 'production'
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
