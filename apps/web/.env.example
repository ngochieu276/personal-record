# Copied into the browser bundle at build time. No trailing slash.
# Local API: http://localhost:4000
# Production API (Fly.io): https://personal-record-api.fly.dev
VITE_API_URL=http://localhost:4000
